name: Analytics Worker Workflow

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Print build message
        run: echo "Building analytics worker..."
      - name: Run build command
        run: |
          echo "Building analytics worker..."
          echo "Build complete"